// Drizzle + Cloudflare D1 client.
// Runtime: drizzle(getCloudflareContext().env.DB) via drizzle-orm/d1 (P4).
// Schema + migrations land in P1 (T1.1). Placeholder keeps the module path stable.
export {};
